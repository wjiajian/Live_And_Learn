/**
* Xor gate:
* if a=0 AND b=0, out 0
* if a=1 AND b=1, out 0
* otherwise out 1
* Xor = And(Or(a, b), Nand(a, b))
*/

CHIP Xor{
    IN a,b;
    OUT out;

    PARTS:
    Or(a=a, b=b, out=o1);
    Nand(a=a, b=b, out=o2);
    And(a=o1, b=o2, out=out);
}