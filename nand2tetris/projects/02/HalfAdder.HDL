/**
HalfAdder
adds two bits and produces a sum and carry bit.
Inputs: a, b
Outputs: sum, carry
Sum = a XOR b
Carry = a AND b
*/

CHIP HalfAdder{
    IN a,b;
    OUT sum, carry;

    PARTS:
    Xor(a=a, b=b, out=sum);
    And(a=a, b=b, out=carry);
}